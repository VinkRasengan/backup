# Render Redirects for FactCheck App - Production
# CRITICAL: Order matters! More specific rules first, catch-all last

# API routes - proxy to backend
/api/* https://api-gateway-3lr5.onrender.com/api/:splat 200

# Health check
/health https://api-gateway-3lr5.onrender.com/health 200

# SPA fallback - MUST BE LAST RULE
# All other routes serve React app (including /community, /chat, /admin, etc.)
/* /index.html 200
