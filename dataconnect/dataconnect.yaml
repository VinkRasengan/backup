specVersion: "v1"
serviceId: "backup"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "backup-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
